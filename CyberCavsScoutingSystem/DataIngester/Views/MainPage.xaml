<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:DataIngester.Views"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             x:Class="DataIngester.Views.MainPage"
             x:DataType="views:MainPage">

	<VerticalStackLayout
            Spacing="0"
            Padding="0"
            VerticalOptions="Center">

		<Label
			Text="Target Directory"/>

		<views:FileView
			BindingContext="{Binding Path=TargetFile}"/>

		<CollectionView
			ItemsSource="{Binding Path=SourceFiles}">

			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="views:FileViewModel">
					<views:FileView/>
				</DataTemplate>
			</CollectionView.ItemTemplate>

		</CollectionView>

		<CollectionView
			ItemsSource="{Binding Path=LogMessages}">
			
			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="system:String">
					<Label
						Text="{Binding Source={RelativeSource Self}, Path=BindingContext}"/>
				</DataTemplate>
			</CollectionView.ItemTemplate>

		</CollectionView>

	</VerticalStackLayout>

</ContentPage>