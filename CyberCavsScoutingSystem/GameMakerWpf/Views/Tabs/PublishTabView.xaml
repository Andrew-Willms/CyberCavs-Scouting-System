<appManagement:AppManagerDependent x:Class="GameMakerWpf.Views.Tabs.PublishTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:GameMakerWpf.Views.Tabs"
             xmlns:appManagement="clr-namespace:GameMakerWpf.AppManagement"
             xmlns:displayData="clr-namespace:GameMakerWpf.DisplayData"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:PublishTabView, IsDesignTimeCreatable=True}">

	<appManagement:AppManagerDependent.Resources>
		<displayData:PublishTabData x:Key="PublishTabData"/>
	</appManagement:AppManagerDependent.Resources>

	<Grid Margin="5,10,5,5">
            
		<Button
			Content="{Binding PublishButtonText, Source={StaticResource PublishTabData}}"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
			Padding="5,3,5,3"
			IsEnabled="{Binding GameIsValid}"
			Click="PublishButton_OnClick"/>

    </Grid>

</appManagement:AppManagerDependent>