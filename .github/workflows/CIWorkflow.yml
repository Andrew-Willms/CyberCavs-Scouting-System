name: .NET

on:
  push:
    branches: [ dev ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: Build
      run: dotnet build CyberCavsScoutingSystem.sln
      working-directory: CyberCavsScoutingSystem
    - name: Test CCSSDomain
      run: dotnet test CCSSDomain.Tests.csproj --no-build --verbosity normal
      working-directory: CyberCavsScoutingSystem/CCSSDomain.Tests
